import java.util.*;
import java.math.*;
@SuppressWarnings("unused")
public class statistics{
   static void fun(){
   }
   public static void main(String[] args){
      /* Map<Character,Integer>map=new HashMap<>();
      String st="statistics";//LinkedHashMap
      for(int i=0; i<st.length(); i++){
         if (map.containsKey(st.charAt(i))){
            int value=map.get(st.charAt(i))+1;
            map.put(st.charAt(i),value);
         }
         else{
            map.put(st.charAt(i),1);
         }
      }
      for(Map.Entry<Character,Integer>entity:map.entrySet()){
         if(entity.getValue()==1){
            System.out.println(entity.getKey());
         }
      }
      System.out.println(map);
      for(int i=0; i<st.length(); i++){
         if (map.get(st.charAt(i))==1){
            System.out.println(st.charAt(i));
            break;
         }
      }*/
      fun();
      //
   }
}