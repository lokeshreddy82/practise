import java.util.Iterator;
import java.util.HashSet;
import java.util.HashMap;
import java.util.ArrayList;
import java.util.Collections;
public class mango{
   /**
    * @param args
    */
   public static void main(String[] args){
      /* HashSet<Integer>set=new HashSet<Integer>();
      HashMap<Integer,String>map=new HashMap<>();
      ArrayList<Integer>arr=new ArrayList<>();
      set.add(1);
      set.add(2);
      set.add(3);
      set.add(4);
      set.add(5);
      set.add(6);
      set.remove(5);
      System.out.println("Printing the set data structue "+set+" and length:"+set.size());
      Iterator<Integer> it=set.iterator();
      while (it.hasNext()){
         System.out.println(it.next());
      }
      System.out.println("Completed set operations");
      arr.add(1);
      arr.add(2);
      arr.add(3);
      arr.add(4);
      arr.add(5);
      arr.add(6);
      arr.add(7);
      arr.add(8);
      arr.add(1);
      arr.add(2);
      System.out.println(arr.get(0));
      arr.set(0,10);
      arr.remove(1);
      System.out.println(arr);
      Collections.sort(arr);
      for(int i : arr){
         System.out.println(i);
      }
      Iterator<Integer> lm=arr.iterator();
      while(lm.hasNext()){
         System.out.println(lm.next());
      }
      Iterator<Integer> kl=set.iterator();
      while(kl.hasNext()){
         System.out.println(kl.next());
      }
      int[] array={1,2,3,4,5,1,3,4,6};
      HashMap<Integer,Integer>name=new HashMap<>();
      for(int ti:array){
         int value=name.getOrDefault(ti, 0)+1;
         name.put(ti,value);
      }
      //it is working Fine
      String s="5";
      Integer l=Integer.parseInt(s);
      int lokesh[]= new int[14];
      System.out.println(name);*/
      
   }
}